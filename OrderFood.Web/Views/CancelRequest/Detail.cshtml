@model OrderFood.Entity.Models.CancelRequest
@{
    ViewBag.Title = "Membership Cancellation Request Details";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<section id="container" class="">
    <section id="main-content">
        <section class="wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <section class="panel">
                        <header class="panel-heading">
                            @ViewBag.Title  - <a href="~/CancelRequest/kurtulusocL" class="btn btn-primary btn-xs">Active Cancellation Request List</a>
                            @if (User.IsInRole("Admin") || User.IsInRole("Asistant"))
                            {
                                <span> - <a href="~/CancelRequest/Delete/@Model.Id" class="btn btn-danger btn-xs" onclick="return confirm('Are you sure ?')"> Delete Request</a></span>
                            }
                            else
                            {
                                <span>you have no authority.</span>
                            }
                        </header>
                        <table class="table table-striped table-advance table-hover">
                            <thead>
                                <tr>
                                    <th>Request Owner</th>
                                    <th>Detail</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    <td>@Model.NameSurname</td>
                                    <td>@Model.Detail</td>
                                </tr>
                            </tbody>
                        </table>
                    </section>
                </div>
            </div>
        </section>
    </section>
</section>