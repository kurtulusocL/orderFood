@model OrderFood.Entity.Models.Complain
@{
    ViewBag.Title = "Complain Detail Information";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<section id="container" class="">
    <section id="main-content">
        <section class="wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <section class="panel">
                        <header class="panel-heading">
                            @ViewBag.Title  - <a href="~/Complain/kurtulusocL" class="btn btn-primary btn-xs">Active Complain List</a>@if (User.IsInRole("Admin") || User.IsInRole("Asistant"))
                            {
                                <span> - <a href="~/Complain/Delete/@Model.Id" class="btn btn-danger btn-xs" onclick="return confirm('Are you sure ?')"> Delete Complain</a></span>
                            }
                            else
                            {
                                <span>you have no authority.</span>
                            }
                        </header>
                        <table class="table table-striped table-advance table-hover">
                            <thead>
                                <tr>
                                    <th>Phone No</th>
                                    <th>Complain Text</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    <td>@Model.PhoneNumber</td>
                                    <td>@Model.Text</td>
                                </tr>
                            </tbody>
                        </table>
                    </section>
                </div>
            </div>
        </section>
    </section>
</section>