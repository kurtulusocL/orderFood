@model OrderFood.Data.Identity.ApplicationUser
@{
    ViewBag.Title = "Company Detail Information";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<section id="container" class="">
    <section id="main-content">
        <section class="wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <section class="panel">
                        <header class="panel-heading">
                            @ViewBag.Title  - <a href="~/Admin/CompanyList" class="btn btn-primary btn-xs">Active Company List</a> -@if (User.IsInRole("Admin") || User.IsInRole("Asistant"))
                            {
                                <a href="~/Admin/DeleteCompany/@Model.Id" class="btn btn-danger btn-xs" onclick="return confirm('Are you sure ?')"> Delete Company</a>
                            }
                            else
                            {
                                <span>you have no authority.</span>
                            }
                        </header>
                        <table class="table table-striped table-advance table-hover">
                            <thead>
                                <tr>
                                    <th>Province</th>
                                    <th>City</th>
                                    <th>Country</th>
                                    <th>Phone Number</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    <td>@Model.Province</td>
                                    <td>@Model.City</td>
                                    <td>@Model.Country</td>
                                    <td>@Model.PhoneNumber</td>
                                </tr>
                            </tbody>
                        </table>
                    </section>
                </div>
            </div>
        </section>
    </section>
</section>